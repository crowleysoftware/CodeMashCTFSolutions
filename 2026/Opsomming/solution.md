# Opsomming

Use Kali Dirb to find valid paths.

1. Dirb's first attempt will find `/ct`
2. Have Dirb try again, starting from `/ctf{`
3. It may be faster to guess the rest of the word once Dirb has found a couple of characters.
